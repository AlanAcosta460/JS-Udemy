const producto9 = 'Monitor 20 pulgadas';

// ** toLowerCase **
console.log(producto9.toUpperCase()); // Transformar a Mayúsculas

// ** toUpperCase **
console.log(producto9.toLowerCase()); // Transformar a Minúsculas

// ** toString **
const valor = 10;
console.log(valor); // Número
console.log(valor.toString()); // String
